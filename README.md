# 🎵 FMR Music — Frontend Specification

> **P.S. / Примечание:**  
> Данный проект создан в учебных целях и представляет собой Техническое Задание (ТЗ) и реализацию фронтенда для музыкального портала **FMR Music**.

---

## 📖 О проекте
**FMR Music** — это современное одностраничное веб-приложение (SPA) для стриминга музыки. Интерфейс спроектирован с упором на визуальную привлекательность ("Wow-эффект"), плавные анимации и удобство использования на мобильных устройствах.

## 🛠 Технологический стек

Проект реализован на чистых технологиях без использования тяжелых JS-фреймворков для максимальной производительности и простоты обучения.

*   **HTML5** — Семантическая верстка.
*   **CSS3** — Custom Properties (Variables), Flexbox, CSS Grid, Media Queries.
*   **JavaScript (ES6+)** — Модульная логика, SPA-навигация, управление состоянием плеера.
*   **Bootstrap 5** — Используется только Grid System и Reset (подключен через CDN).
*   **Bootstrap Icons** — Иконочный шрифт.
*   **Google Fonts** — Основной шрифт **Inter**.

---

## 🎨 Дизайн-система

### 1. Цветовая палитра (Light Purple Theme)

Дизайн выполнен в светлых, воздушных тонах с яркими фиолетовыми акцентами.

*   **Фон страницы (`--bg-primary`):** `#f5f3ff` (Очень светлый лавандовый)
*   **Карточки (`--bg-card`):** `#ffffff` (Чистый белый)
*   **Основной акцент (`--accent`):** `#7c3aed` (Насыщенный фиолетовый)
*   **Градиент бренда:** `linear-gradient(135deg, #7c3aed, #a78bfa)`
*   **Текст основной:** `#1a1a2e` (Почти черный)
*   **Текст вторичный:** `#6b6b8c` (Серо-фиолетовый)

### 2. Типографика
Используется шрифт **Inter** для чистого и современного вида.
*   **Заголовки:** Bold (700/800)
*   **Наборный текст:** Regular (400) / Medium (500)

### 3. UI Компоненты

*   **Glassmorphism:** Используется в плеере и мобильной навигации (`backdrop-filter: blur`).
*   **3D Tilt Cards:** Карточки треков и альбомов имеют эффект наклона при наведении мыши (JS + CSS Transform).
*   **Quick Cards:** Быстрые карточки навигации с градиентными иконками.
*   **Track Items:** Списки треков с hover-эффектами, кнопками лайка и контекстным меню ("три точки").

---

## 📱 Адаптивность и Мобильная версия

Проект разработан с подходом **Mobile-First**.

### Мобильные особенности (≤ 768px):
1.  **Фиксированная навигация:** Нижнее меню (`Bottom Navigation`) закреплено внизу экрана.
    *   Структура: `Главная | Треки | Жанры | Каналы | Нравится`
2.  **Профиль:** Кнопка профиля перенесена в **Header**.
3.  **Swipes:** Поддержка свайпов влево/вправо для переключения между разделами.
4.  **Safe Area:** Учтены отступы для iPhone (Home Indicator) через `env(safe-area-inset-bottom)`.

### Десктопная версия (≥ 769px):
1.  **Сайдбар:** Полноценная боковая навигация слева.
2.  **Плеер:** Расширенная панель плеера с регулировкой громкости.
3.  **Сетка:** Контент перестраивается в 3-6 колонок в зависимости от ширины экрана.

---

## 📂 Структура проекта

```
fmr_front/
├── css/
│   └── styles.css      # Все стили проекта (Variables, Reset, Layout, Components)
├── js/
│   └── app.js          # Логика приложения (Player, Navigation, UI Interactions)
├── index.html          # Главный и единственный HTML файл
└── README.md           # Документация
```

---

## ⚙️ Функционал (Frontend Logic)

### 1. SPA Навигация
Переходы между разделами происходят без перезагрузки страницы.
*   Используются `data-page` атрибуты для связывания ссылок и секций.
*   Активная секция получает класс `.active`, остальные скрываются.

### 2. Аудио Плеер
*   Статичный, персистентный плеер (не прерывается при навигации).
*   Mock-данные треков (имитация БД).
*   Функции: Play/Pause, Next/Prev, Progress Bar, Volume (Desktop).
*   Анимация обложки и "пульсация" при воспроизведении.

### 3. Контекстное меню
*   Кнопка "Три точки" у каждого трека.
*   Выпадающее меню с опциями (Добавить в плейлист, Скачать и т.д.).
*   Автоматическое позиционирование, чтобы меню не уходило за край экрана.

---

## 🚀 Запуск

Для запуска проекта достаточно любого статического сервера, так как проект не требует сборки.

**Вариант 1 (Простой):**
Просто откройте файл `index.html` в любом современном браузере.
*   Двойной клик по файлу в проводнике.
*   Или перетащите файл в окно браузера.
*   **Примечание:** Все функции (навигация, плеер) работают корректно без сервера, так как не используются модули (`type="module"`) или AJAX-запросы.

**Вариант 2 (VS Code Live Server):**
1.  Установите расширение **Live Server**.
2.  Откройте `index.html`.
3.  Нажмите "Go Live".

**Вариант 3 (Python):**
```bash
python -m http.server 8000
# Откройте http://localhost:8000
```
